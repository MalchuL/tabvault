# Unassigned is a virtual collection

A null group identity means Unassigned rather than identifying a persisted Group. Clients present
Unassigned as a virtual collection when it contains tabs; active tabs may be Unassigned and every
archived tab is Unassigned. This keeps records without placement visible without reserving a Group
identity.
