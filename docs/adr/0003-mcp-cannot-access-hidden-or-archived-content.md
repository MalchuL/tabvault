# MCP cannot access hidden or archived content

MCP exposes only active, ordinarily visible content, even when an agent already knows a hidden or
archived record ID. Human clients manage those lifecycle states through dedicated Hidden and
Archive views. This keeps agent discovery and mutation within the ordinary working library while
leaving visibility and recovery controls available in the UI.
