# Saved tabs are single-placement occurrences

A Saved Tab records one save occurrence, so several records may share a saved URL and differ in
metadata. A Saved Tab belongs to at most one Group; saving the same URL again creates another
occurrence rather than sharing or moving the first record. This preserves capture context and makes
duplicate reduction an explicit user operation.
