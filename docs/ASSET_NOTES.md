# Documentation Asset Notes

- `tabvault-dashboard_4a54e92c.png` is a verified desktop dashboard capture. It visibly includes the collection rail, active Inbox marker, API connection card, Semantic model panel, Index health controls, Local alerts/Quiet mode card, search surface, and tab-list view controls.
- `tabvault-system-map_0017074b.png` is rendered from `tabvault-system-map.mmd`. It shows browser-local and Chrome-local clients connecting with bearer authentication to FastAPI; the API’s derived semantic index; and the MCP bridge’s authenticated proxy relationship.
