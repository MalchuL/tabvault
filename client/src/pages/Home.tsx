import { LibraryWorkspace } from "@/domain/library/components/LibraryWorkspace";

/** Render the shared library workspace for every library route. */
export default function Home() {
  return <LibraryWorkspace />;
}
